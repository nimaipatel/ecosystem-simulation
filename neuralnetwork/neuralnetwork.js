class NeuralNetwork {
    constructor(inputNeurons, hiddenNeurons, outoutNeurons) {
        this.inputLayer = new Array(inputNeurons)
        this.outoutLayer = new Array(hiddenNeurons)
        this.hiddenLayer = new Array(outoutNeurons)
    }

    forwardPass(input){
        
    }
}